# 目的・ゴール (Goal)
※ 何を達成したいか、どのような状態にしたいかを記載してください。
例: `mise` を使ってインストールされるツール群の設定を別ファイルに切り出したい。

# 背景・コンテキスト (Context)
※ なぜその変更が必要なのか、前提となる情報があれば記載してください。
例: 現在はセットアップスクリプトに直接書かれているが、管理を容易にするために `mise.toml` へ一覧をまとめたい。

# 現在の挙動・課題 (Current Behavior / Issue)
※ バグ修正やリファクタリングの場合、現状の問題点を記載してください。
例: `setup-Ubuntu-native.sh` を実行すると `mise use --global` が多数実行されてしまう。

# 期待する挙動・要件 (Expected Behavior / Requirements)
※ どのような動作を期待するか、また守るべき制約（使いたいライブラリ、守るべきコーディング規約・パスなど）があれば記載してください。
- 要件1: 
- 要件2: 

# 対象ファイル・ディレクトリ (Target Files / Directories)
※ 変更を加えたいファイルや、解析してほしいディレクトリがあれば記載してください。
- `path/to/script.sh`
- `path/to/config.toml`

# その他・参考情報 (Notes / References)
※ エラーログ、参考URL、具体的なコードの断片などがあればこちらに貼り付けてください。
```
エラーログやコード
```
