# Install
```shell
sudo apt install git
git clone git@github.com:vios-fish/dotfiles
```

## Devbox

To set up the environment using [devbox](https://www.jetpack.io/devbox):

```shell
devbox shell
```

This will install necessary packages and set up dotfiles automatically.